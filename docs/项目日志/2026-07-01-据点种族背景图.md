# 《史诗起点》项目日志：据点种族背景图

生成时间：2026-07-01

## 1. 更新范围

StrongholdUI 面板背景图系统，根据玩家种族切换对应据点背景。

## 2. 已完成内容

### 2.1 StrongholdUI 背景图字段

- 新增 `backgroundImage` (Image)、`humanStrongholdBg`、`heavenStrongholdBg`、`ghostStrongholdBg` 四个 public 字段
- 新增 `GetBackgroundForRace(RaceType)` 方法，按种族返回对应背景 Sprite
- `Open()` 中调用 `GetBackgroundForRace()` 设置 `backgroundImage.sprite`
- 修改文件：`Assets/Scripts/Scenes/Main/UI/StrongholdUI.cs`

## 3. Unity 场景接线要点

- StrongholdUI 组件 Inspector 中 **据点背景图（每种族1张）** 区块
- 将 panel 下的背景 Image 节点拖入 `Background Image`
- 将三张种族据点背景图 Sprite 分别拖入对应字段

## 4. 验证记录

- 已执行 `dotnet build .\Epic_Origin.sln --no-restore`，结果为 0 警告、0 错误。
- Play 模式测试通过，据点面板背景图正确按种族切换。

## 5. 后续事项

- 无
